### Optimizing Nonlinear Hazard Models in Survival Analysis Using Nature Inspired Algorithms

"modelling non-linear covariate effects in Cox proportional hazards models - Self-Adaptive Fractional-polynomial Evolution for Cox models"

## Installation

First, clone the repository and navigate into the project folder. Then install the required Python packages using:

```bash
pip install -r requirements.txt

